#This file uses the c++11 for compilation. If you are using other version it may create problem
# The executable is in build directory
make #for compiling the code. 

chmod +x run.sh 

./run.sh 0 For running the program
./run.sh 1 For running the program using optirun (on optimus systems)
./run.sh 2 For running tests
./run.sh 3 For generate humanoid model
./run.sh 4 For generate r2d2 model

Alternatively, you can cd into the `build' directory and run either
./assignment_03
or
optirun ./assignment_03 

For a full report please see:
http://anshumankumar.github.io/CS475/assignment3.html

